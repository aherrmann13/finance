GetAccountValueResponse:
  description: ""
  content:
    application/json:
      schema:
        type: array
        items:
          $ref: "../schemas/reporting.yaml#/AccountValue"

GetNetWorthResponse:
  description: ""
  content:
    application/json:
      schema:
        type: number
  
